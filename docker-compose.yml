version: '3.8'
services:
  api:
    build: ./accounting
    ports:
      - "3001:3001"
    volumes:
      - ./accounting/data:/usr/src/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped
